import { HoldingFormat } from "./index";

export default {
  title: "Components/HoldingFormat",
  component: HoldingFormat,
};

const Template = (args) => <HoldingFormat {...args} />;
export const Default = Template.bind({});
Default.args = {};
